import { Section, SectionTitle } from "@/components/elements/section";
import { Articles } from "@/components/elements/articles";

export default function Page() {
    return <Section>
        <SectionTitle>
            blog
        </SectionTitle>
        <Articles />
    </Section>
}